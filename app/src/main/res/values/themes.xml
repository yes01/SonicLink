<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme with Material Design 3 -->
    <style name="Theme.Sonicandroidapk" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorPrimaryVariant">@color/brand_primary_dark</item>
        <item name="colorOnPrimary">@color/text_on_primary</item>
        <item name="colorSecondary">@color/brand_secondary</item>
        <item name="android:statusBarColor">@color/bg_page</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>
        <item name="android:windowBackground">@color/bg_page</item>
        <item name="materialAlertDialogTheme">@style/ThemeOverlay.SonicLink.MaterialAlertDialog</item>
        <item name="snackbarStyle">@style/Widget.SonicLink.Snackbar</item>
        <item name="snackbarTextViewStyle">@style/Widget.SonicLink.Snackbar.TextView</item>
        <item name="snackbarButtonStyle">@style/Widget.SonicLink.Snackbar.Button</item>
        <!-- Simplify ripple effects globally for cleaner look -->
        <item name="android:colorControlHighlight">#1A000000</item>
    </style>

    <!-- Transparent Activity Theme (for permission dialogues) -->
    <style name="Theme.Sonicandroidapk.Transparent" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.4</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- Fullscreen Media Viewer Theme -->
    <style name="Theme.Sonicandroidapk.Fullscreen" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@color/black</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
